/* import series from "../series.js";

 const watchedSeriesCounter = () => {
  let counterWatched = 0;
  let counterPending = 0;
  series.forEach((serie) => {
    if (serie.watched === true) {
      counterWatched++;
    } else {
      counterPending++;
    }
  });
}; */
